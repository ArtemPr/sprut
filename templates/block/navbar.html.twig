<div class="navbar-expand-md">
    <div class="collapse navbar-collapse" id="navbar-menu">
        <div class="navbar navbar-light">
            <div class="container-xl">
                <ul class="navbar-nav">
                    {% if auth.auth_admin|default(false) %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('main') }}">
                                <span class="nav-link-icon d-md-none d-lg-inline-block">
                                  <svg xmlns="http://www.w3.org/2000/svg" class="icon" width="24" height="24" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round"><path stroke="none" d="M0 0h24v24H0z" fill="none"/><polyline points="5 12 3 12 12 3 21 12 19 12" /><path d="M5 12v7a2 2 0 0 0 2 2h10a2 2 0 0 0 2 -2v-7" /><path d="M9 21v-6a2 2 0 0 1 2 -2h2a2 2 0 0 1 2 2v6" /></svg>
                                </span>
                                <span class="nav-link-title">
                                    Рабочий стол
                                </span>
                            </a>
                        </li>
                    {% endif %}
                    {% if auth.auth_program|default(false) or auth.auth_directory|default(false) %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown" data-bs-auto-close="outside" role="button" aria-expanded="false">
                                <span class="nav-link-icon d-md-none d-lg-inline-block">
                                  <svg xmlns="http://www.w3.org/2000/svg" class="icon" width="24" height="24" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round"><path stroke="none" d="M0 0h24v24H0z" fill="none"/><polyline points="12 3 20 7.5 20 16.5 12 21 4 16.5 4 7.5 12 3" /><line x1="12" y1="12" x2="20" y2="7.5" /><line x1="12" y1="12" x2="12" y2="21" /><line x1="12" y1="12" x2="4" y2="7.5" /><line x1="16" y1="5.25" x2="8" y2="9.75" /></svg>
                                </span>
                                <span class="nav-link-title">
                                    Курсы
                                </span>
                            </a>
                            <div class="dropdown-menu">
                                {% if auth.auth_program|default(false) %}
                                    <a class="dropdown-item" href="{{ path('program') }}">
                                        Программы
                                    </a>
                                {% endif %}
                                {% if auth.auth_directory|default(false) %}
                                    <a class="dropdown-item" href="{{ path('discipline') }}">
                                        Дисциплины
                                    </a>
                                {% endif %}
                            </div>
                        </li>
                    {% endif %}

                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown" data-bs-auto-close="outside" role="button" aria-expanded="false">
                                <span class="nav-link-icon d-md-none d-lg-inline-block">
                                  <svg xmlns="http://www.w3.org/2000/svg" class="icon" width="24" height="24" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round"><path stroke="none" d="M0 0h24v24H0z" fill="none"/><polyline points="12 3 20 7.5 20 16.5 12 21 4 16.5 4 7.5 12 3" /><line x1="12" y1="12" x2="20" y2="7.5" /><line x1="12" y1="12" x2="12" y2="21" /><line x1="12" y1="12" x2="4" y2="7.5" /><line x1="16" y1="5.25" x2="8" y2="9.75" /></svg>
                                </span>
                            <span class="nav-link-title">
                                    Сервисы
                                </span>
                        </a>
                        <div class="dropdown-menu">
                            {% if auth.auth_program|default(false) %}
                                <a class="dropdown-item" href="{{ path('service_antiplagiat') }}">
                                    Антиплагиат
                                </a>
                            {% endif %}
                            {% if auth.auth_directory|default(false) %}
                                <a class="dropdown-item" href="{{ path('service_literra') }}">
                                    Литера 5
                                </a>
                            {% endif %}
                            {% if auth.auth_directory|default(false) %}
                                <a class="dropdown-item" href="{{ path('service_legalregister') }}">
                                    Реестр прав
                                </a>
                            {% endif %}
                        </div>
                    </li>

                    {% if auth.auth_admin|default(false) %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown" data-bs-auto-close="outside" role="button" aria-expanded="false">
                                <span class="nav-link-icon d-md-none d-lg-inline-block"><!-- Download SVG icon from http://tabler-icons.io/i/checkbox -->
                                  <svg xmlns="http://www.w3.org/2000/svg" class="icon" width="24" height="24" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round"><path stroke="none" d="M0 0h24v24H0z" fill="none"/><polyline points="9 11 12 14 20 6" /><path d="M20 12v6a2 2 0 0 1 -2 2h-12a2 2 0 0 1 -2 -2v-12a2 2 0 0 1 2 -2h9" /></svg>
                                </span>
                                <span class="nav-link-title">
                                    Панель управления
                                </span>
                            </a>
                            <div class="dropdown-menu">
                                {% if auth.auth_user|default(false) %}
                                    <a class="dropdown-item" href="{{ path('administrator_user') }}">
                                        Пользователи
                                    </a>
                                {% endif %}
                                {% if auth.auth_directory|default(false) %}
                                    <a class="dropdown-item" href="{{ path('administrator_directory_fgos') }}">
                                        Справочники
                                    </a>
                                {% endif %}
                                {% if auth.auth_log|default(false) %}
                                    <a class="dropdown-item" href="{{ path('administrator_log') }}">
                                        Журнал событий
                                    </a>
                                {% endif %}
                            </div>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </div>
</div>
